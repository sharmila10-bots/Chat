<!DOCTYPE html>
<html>
  <head>
    <title>MYCHATAPPN</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <h2 class="ui center aligned large header">
  Welcome to MessageMe - <em>a complete Chat App</em>
</h2>
<h4 class="ui center aligned medium icon header">
  <i class="circular orange users tiny icon"></i>
  Sign Up Form
</h4><br>
  <div class="ui container">
  <%= form_for(:user, html: {class: "ui form", role: "form"}, url: signup_path) do |f| %>
    <div class="field">
          <%= f.label :username, "Username :" %>
          <div class="ui left icon input">
            <%= f.text_field :username, placeholder: "Username" %>
            <i class="user icon"></i>
          </div>
        </div><br>
        <div class="field">
          <%= f.label :password, "Password :" %>
          <div class="ui left icon input">
            <%= f.password_field :password, placeholder: "Password" %>
            <i class="lock icon"></i>
          </div>
        </div><br>
        <%= f.button "Submit", class: "ui orange submit button" %>
      <% end %>
  </div>  
  </body>
</html>
